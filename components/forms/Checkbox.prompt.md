**Checkbox** — chunky opt-in for agreements, filters, checklists.

```jsx
<Checkbox label="Ережелермен келісемін" defaultChecked />
```

Controlled (`checked`+`onChange`) or uncontrolled (`defaultChecked`). `tone` sets the fill (`brand` amber / `teal`).
