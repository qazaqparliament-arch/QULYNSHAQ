**Select** — styled native dropdown (matches Input). Good for difficulty, age, quantity.

```jsx
<Select label="Жас шамасы" placeholder="Таңдаңыз" options={['4–6 жас','7–9 жас','10+ жас']} />
```

Accepts `options` as strings or `{value,label}`. Shares Input's focus/error styling.
