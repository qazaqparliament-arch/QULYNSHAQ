**Switch** — an on/off preference (sound, autoplay, dark mode).

```jsx
<Switch label="Видеоны автоматты ойнату" defaultChecked />
```

Controlled or uncontrolled. Track turns teal when on; knob slides with a soft bounce.
