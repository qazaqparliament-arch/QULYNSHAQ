**Callout** — a boxed tip, rule, or warning; the booklet's teaching voice on-screen.

```jsx
<Callout tone="rule" title="Ереже">Пілта тек диагональ бойынша жүреді.</Callout>
```

Tones: `tip` (teal), `rule` (ink), `warning` (clay), `fun` (amber). Each has a default glyph; override with `icon`.
