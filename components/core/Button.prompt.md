**Button** — the primary tactile CTA; use for order, start-lesson, and next-step actions.

```jsx
<Button variant="primary" size="lg" iconRight={<span>♞</span>}>Сабақты бастау</Button>
```

Variants: `primary` (amber), `secondary` (teal), `accent` (clay), `outline` (ink cut-line), `ghost` (flat, low emphasis). Sizes `sm|md|lg`. Non-ghost variants render a solid bottom edge that compresses on press. Use `full` to fill width, `iconLeft`/`iconRight` for glyphs (Unicode chess pieces or Lucide icons).
