**Card** — warm paper surface for grouping content; the base for lesson & product tiles.

```jsx
<Card variant="paper" pad="lg" interactive>…</Card>
```

Variants: `paper` (raised, default), `cut` (thick ink cut-line border — echoes a punch-out piece), `sunk` (recessed panel). `pad` sets inner padding; `interactive` adds a hover lift.
